language: ruby
rvm:
  - 1.9.3

env:
  - DB=sqlite

script:
  - RAILS_ENV=test bundle exec rake --trace db:migrate spec
  - RAILS_ENV=test bundle exec rspec spec --tag network_dependent
services:
  - redis-server



